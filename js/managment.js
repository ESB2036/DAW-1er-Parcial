const buttonReturn = document.getElementById("button-return");

buttonReturn.addEventListener("click", function () {
  // Redirige a la página del panel del administrador:
  window.location.href = "../administrator_panel.html";
});
